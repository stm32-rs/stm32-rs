CR1:
  DUALMOD:
    Independent: [0, "Independent mode"]
    RegularInjected:
      [1, "Combined regular simultaneous + injected simultaneous mode"]
    RegularAlternateTrigger:
      [2, "Combined regular simultaneous + alternate trigger mode"]
    InjectedFastInterleaved:
      [3, "Combined injected simultaneous + fast interleaved mode"]
    InjectedSlowInterleaved:
      [4, "Combined injected simultaneous + slow interleaved mode"]
    Injected: [5, "Injected simultaneous mode only"]
    Regular: [6, "Regular simultaneous mode only"]
    FastInterleaved: [7, "Fast interleaved mode only"]
    SlowInterleaved: [8, "Slow interleaved mode only"]
    AlternateTrigger: [9, "Alternate trigger mode only"]
