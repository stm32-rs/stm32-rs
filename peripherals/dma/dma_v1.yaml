# DMA as used on F0, F1, F3 family
#
# Please use `dma_v1_with_remapping.yaml` for devices that has DMA remapping
# functionality such as the L0 and L4 family.

# DMA1 has 7 channels
# DMA2 has 5 channels
"DMA[12]":
  _include:
    - v1.yaml
